# 后端任务清单 (Todolist)

## 🎯 核心目标
支持精简后的用户模型及更精细的数据操作。

## 📝 待办事项
- [ ] **项目维护**
    - [ ]配置 Github 仓库并上传代码。

- [x] **用户模型简化**
    - [x] 数据库模型 `User` 中删除 `Name` 字段。
    - [x] 相应更新数据库 Seed 种子数据及迁移（如适用）。
- [x] **身份验证逻辑更新**
    - [x] 更新 `AuthController` 中所有与 `User.Name` 相关的返回值及入参。
- [x] **数据操作支持**
    - [x] 确保 `DataController` 的批量保存兼容全年度 12 个月的同时录入。

## ✅ 已完成
- [x] 成功迁移 ID 为整数类型。
- [x] 实现 `check-conflict` 冲突检查接口（基于年、月、项、位置）。
- [x] 实现 `change-password` 端点。
